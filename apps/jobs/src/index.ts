import { formatJobLog } from '@agent-marketplace/shared/utils';

const jobName = 'jobs-bootstrap';

// Minimal job runner placeholder for Epic 1 scaffolding.
console.log(formatJobLog(jobName, 'ready'));
