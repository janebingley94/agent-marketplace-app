你是一个“项目功能验收（QA + 技术负责人）助手”。请基于以下 PRD 功能清单，为 Agent Marketplace 项目输出一份“功能验收包”，用于上线前验收与回归测试。

要求：

1) 必须逐条覆盖 F1-F10；按优先级分组（P0/P1/P2）。
2) 对每个功能点输出：
   - 验收用例列表（至少：正向/反向/边界/安全/可用性/兼容性/性能 维度中相关的）
   - 每条用例：前置条件、步骤、期望结果、通过/失败判定、需要的测试数据
   - 需要的接口清单（endpoint、method、关键字段、鉴权方式、错误码）
   - 需要的埋点/日志（用于验收定位问题）
3) 在最后输出：
   - “自动化测试建议”：E2E（Playwright/Cypress）、API（Vitest/Jest + supertest）、合约交互（如有）分别怎么做
   - “回归测试矩阵”：浏览器/设备/网络环境/链环境（mainnet/testnet/local）
   - “风险与缺口清单”：如果 PRD 验收标准不足以落地，请明确列出缺口并给出补充建议
4) 对 Web3 相关验收：必须包含签名登录安全性（nonce、防重放、域名绑定）、链ID校验、钱包未安装/拒签/断连等场景。
5) 输出格式必须是 Markdown，包含表格与清晰的标题层级，便于直接贴到Notion/Jira。

项目功能清单（PRD）：

| # | 功能点 | 验收标准 | 优先级 |
|---|-------|---------|--------|
| F1 | 钱包登录 | 用户可通过MetaMask签名登录，获取JWT | P0 |
| F2 | Marketplace浏览 | 显示Agent列表，支持搜索、筛选、分页 | P0 |
| F3 | Agent详情 | 显示Agent信息、定价、版本历史 | P0 |
| F4 | 创建Agent | 表单验证，成功创建后跳转Studio | P0 |
| F5 | 我的Agents | 显示用户创建的Agent列表 | P0 |
| F6 | Wallet页面 | 显示余额、收益、交易记录 | P1 |
| F7 | Dashboard | 显示收益趋势图、Agent分布 | P1 |
| F8 | Bills列表 | 显示账单列表，支持筛选 | P1 |
| F9 | DAO提案 | 显示提案列表、投票进度 | P2 |
| F10 | Agent运行 | 【Deferred】Runtime执行 | P2 |

额外约束与假设（如你需要可以自行补充，但必须明确写出“假设”）：

- 登录方式：MetaMask personal_sign 或 eth_signTypedData_v4（二选一，若无法确定请给出两套验收与实现建议）
- JWT：需要说明 token 生命周期、刷新机制（若未知请列为缺口并建议）
- Marketplace：数据来自后端 API（若未知请给出 RESTful 设计建议与验收依赖）
- Studio：创建Agent成功后的落地页（路由）请给出可验收的跳转判定方式（URL/页面标题/关键元素）
- Bills / Wallet / Dashboard 数据结构若未知：请先提出最小可用字段集合（MVP schema）来支撑验收

输出的第一部分请先给“总览验收策略”，然后按 P0 → P1 → P2 输出详细验收用例。
